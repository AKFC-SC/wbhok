{
  "name": "akfc-match-hub-api",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: secure SportMonks -> Match Hub API proxy",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
